export interface Venue {
    id: number;
    name: string;
    address: string;
    createdAt: string;
    updatedAt: string;
  }
  
  export interface VenueFormData {
    name: string;
    address: string;
  }